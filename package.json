{
  "name": "authmatic",
  "version": "0.1.0",
  "private": true,
  "description": "An autonomous agent that files prior authorizations on real payer portals — Applied Intelligence Hackathon, 31 May 2026",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n web,agent -c blue,magenta \"pnpm --filter authmatic-web dev\" \"cd apps/agent && .venv/bin/uvicorn main:app --reload --port 8000\"",
    "build": "pnpm --filter authmatic-web build",
    "smoke": "bash scripts/smoke.sh",
    "seed": "bash scripts/seed.sh",
    "reset": "bash scripts/reset.sh"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
